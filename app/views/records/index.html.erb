<h1>records</h1>

<%= form_tag search_records_path, remote: true, id: 'search_artists' do %>
  <%= label_tag :artist %>
  <%= search_field_tag :artist %>
  <%= submit_tag 'search', id: 'search' %>
<% end %>

<button class="clear">clear</button>

<section class="records"></section>

<section class="collection">

  <%= render 'index' %>

</section>
