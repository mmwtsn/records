<% @records.each do |record| %>
<article class="record">
  <%= record.title %>
  <%= link_to 'view' %>
  <%= link_to 'edit' %>
  <%= link_to 'delete', record_path(record.id), method: :delete, remote: true %>
</article>
<% end %>
