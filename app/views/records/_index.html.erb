<% @records.each do |record| %>
<article class="record">
  <%= record.title %>
  <%= link_to 'view', record_path(record.id), class: 'view-record' %>
  <%= link_to 'delete', record_path(record.id), method: :delete, remote: true, class: 'delete-record' %>
</article>
<% end %>
